let useCharacterAsCursor=(function(){let size=24,canvas,context;canvas=document.createElement("canvas");canvas.width=size;canvas.height=size;context=canvas.getContext("2d");context.font=size+"px sans-serif";context.textBaseline="middle";context.textAlign="center";return function(character){if(!character){document.body.style.cursor="default";return}
    context.clearRect(0,0,canvas.width,canvas.height);context.fillText(character,size/2,size/2);let imgDataURL=canvas.toDataURL();let offset=(size/2)+" 0";document.body.style.cursor="url("+imgDataURL+") "+offset+", auto"}}());function setup(){let date=new Date();let month=date.getMonth()+1;let logo=document.getElementById('logo');let logoName='def';if(month===6){logoName='6'}
    logo.setAttribute('src','/assets/img/brand/logo-'+logoName+'.svg');logo.setAttribute('onerror','this.onerror=null;'+'this.src=\'/assets/img/brand/logo-'+logoName+'.png\'')
    let copy=document.getElementById('copyright-notice');copy.innerHTML='Copyright '+date.getFullYear()+' © All Rights Reserved';let years=document.getElementById('age-text');years.innerHTML=date.getFullYear()-1997}
function loadParticles(){particlesJS.load('particles-bg','/assets/config/particles.json',function(){console.log('Particles ready! :D')})}
document.addEventListener('DOMContentLoaded',function(){let $navbarBurgers=Array.prototype.slice.call(document.querySelectorAll('.navbar-burger'),0);if($navbarBurgers.length>0){$navbarBurgers.forEach(function($el){$el.addEventListener('click',function(){let target=$el.dataset.target;let $target=document.getElementById(target);$el.classList.toggle('is-active');$target.classList.toggle('is-active')})})}
    setup();loadParticles();useCharacterAsCursor('🌀')})